#include<stdio.h>
#include<conio.h>
void main()
{
    int n,i,a[i];
    clrscr();
    printf("enter the array size");
    scanf("%d",&n);
    printf("enter the array value");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        }
for(i=0;i<n;i++)
{
printf("%d %d\n",i,a[i]);
}
    getch();
}
